<nav class="navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">FilmSpot</a>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Movies', movies_path %></li>
        <li><%= link_to 'Directors', directors_path %></li>
        <li><%= link_to 'Actors', actors_path %></li>
      </ul>
      <form class="navbar-form navbar-right" action="/login" method="POST">
        <% if !user_signed_in? %>
          <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-success btn-orange' %>
        <% elsif user_signed_in? %>

          <% if current_user.name %>
            <div class="navbar-div">Logged in as <%= current_user.name %></div>
          <% else %>
            <div class="navbar-div">Logged in as <%= current_user.email %></div>
          <% end %>

          <%= link_to 'Sign Out', destroy_user_session_path, class: 'btn btn-success btn-orange', method: :delete %>
        <% end %> <!-- /if !user_signed_in? -->
      </form>
    </div><!--/.navbar-collapse -->

  </div> <!-- /container -->
</nav>
<nav class="navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">FilmSpot</a>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      <form class="navbar-form navbar-right" action="/login" method="POST">
        <% if !user_signed_in? %>
          <div class="form-group">
            <input type="text" name="name" placeholder="Name" class="form-control">
            <input type="password" name="password" placeholder="Password" class="form-control">
            <input type="submit" value="Sign In" class="btn btn-success">
          </div>

          

          <!-- <a href="/login" class="btn btn-default nav-btn" type="submit">Sign in</a> -->
        <% elsif user_signed_in? %>
          <div class="navbar-div">Logged in as <%= current_user.email %></div>
          <a href="/logout" type="submit" class="btn btn-default nav-btn">Sign Out</a>
        <% end %>
      </form>
    </div><!--/.navbar-collapse -->

  </div> <!-- /container -->
</nav>